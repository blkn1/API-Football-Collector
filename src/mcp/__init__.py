"""
MCP (Model Context Protocol) layer.

This package exposes a stdio MCP server (for Claude Desktop) with READ-ONLY tools
to query system status (coverage, rate limits, job status) and core football data.
"""


